<view class="container">
  <view class="card phone-card" wx:if="{{user}}">
    <view>
      <view class="section-title">登录手机号</view>
      <view class="paragraph">{{user.phoneBound ? user.phoneNumberMasked : '尚未验证'}}</view>
    </view>
    <view class="verified">{{user.phoneBound ? '已验证' : '未验证'}}</view>
  </view>
  <view class="card privacy-card">
    <view class="section-title">隐私说明</view>
    <view class="paragraph">手机号仅用于账号验证、安全风控和账号申诉，社区公开页面不会展示完整号码。</view>
    <view class="paragraph">项目保存实现社区功能所需的账号标识、公开资料、宠物档案、动态和互动数据。图片存储在配置的 CloudBase 环境中。</view>
    <view class="paragraph">内容安全检查由服务端发起，前端不保存 AppSecret 或管理员密钥。</view>
    <view class="privacy-link" bindtap="openPrivacy">查看完整隐私保护说明 ›</view>
  </view>
  <view class="card menu">
    <view class="menu-item" bindtap="clearCache"><text>清理本地缓存</text><text class="arrow">›</text></view>
    <view class="menu-item danger" bindtap="deleteAccount"><text>注销账号</text><text class="arrow">›</text></view>
  </view>
</view>
